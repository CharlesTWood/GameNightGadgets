To start the server 'python run.py'

Create DB
from website import db
db.create_all()

How to update the DB:

https://flask-migrate.readthedocs.io/en/latest/

DB commands
Creating user in SQLite using Python:

virtualenv pydev
pydev\Scripts\activate

print to console using stout
print('Hello world!', file=sys.stderr)

How DB relationships work in sqlalchemy: https://docs.sqlalchemy.org/en/14/tutorial/orm_related_objects.html

TEST USER and APPEND the address, and product(sqlalchemy):
from website.models import User, Mailing_Address_Table, Product
from website import db
user = User(username = "TomRiddle", email = "tom@riddle.com", password = "harrysucks")
address = Mailing_Address_Table(name = "Tome Riddle", address = "The old Riddle estate", city = "poopyland", organization = "Hogwarts School of Witchcraft and Wizardry", state = "NC", zip = 25446)
product = Product(name = "Horcrux creation for dummies", price = 5.50, product_details = "Series 1 of 7, difficulty: moderate", description = "Create immortality by parsing your soul into other objects!", release_date="01/01/01")
user.addresses.append(address)
db.session.add(user)
db.session.commit()

name = db.Column(db.String(20), unique=False, nullable=False)
price = db.Column(db.Numeric)
product_details = db.Column(db.String)
description = db.Column(db.String(), unique=False, nullable=False)
pre_order = db.Column(db.Boolean)
date_added = db.Column(db.Date, server_default=func.now())
release_date = db.Column(db.DateTime, nullable=True)