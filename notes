To start the server 'python run.py'

Create DB
from website import db
db.create_all()

How to update the DB:

https://flask-migrate.readthedocs.io/en/latest/

DB commands
Creating user in SQLite using Python:

virtualenv pydev
pydev\Scripts\activate

print to console using stout
print('Hello world!', file=sys.stderr)

How DB relationships work in sqlalchemy: https://docs.sqlalchemy.org/en/14/tutorial/orm_related_objects.html

TEST USER and APPEND the address (sqlalchemy):
from website.models import User, Mailing_Address_Table
from website import db
user = User(username = "TomRiddle", email = "tom@riddle.com", password = "harrysucks")
address = Mailing_Address_Table(name = "Tome Riddle", address = "101 Hogwarts Lane", city = "poopyland", organization = "Hogwarts School of Witchcraft and Wizardry", state = "NC", zip = 25446)
user.addresses.append(address)
db.session.add(user)
db.session.commit()